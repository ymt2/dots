;; redo+
;; (auto-install-from-emacswiki "redo+.el")
;; (auto-install-from-url "http://www.emacswiki.org/emacs/download/redo+.el")
(require 'redo+)
(setq undo-no-redo t)
(setq undo-limit 60000)
(setq undo-strong-limit 90000)
